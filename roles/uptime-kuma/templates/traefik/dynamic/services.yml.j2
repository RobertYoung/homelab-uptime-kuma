http:
  routers:
    uptime-kuma:
      rule: "Host(`{{ service_domain }}`)"
      entrypoints:
        - websecure
      service: uptime-kuma
      tls: {}

  services:
    uptime-kuma:
      loadBalancer:
        servers:
          - url: "http://uptime-kuma-app:3001"
